- type: http
  enabled: true
  id: elastic-rest-api
  name: Elastic REST API
  urls:
      - "http://elasticsearch:9200"
  username: "elastic"
  password: ${ELASTIC_PASSWORD}
  schedule: "@every 1s"
